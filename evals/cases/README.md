# Evaluation Cases

Cases are small and deterministic. Each case states expected detection, right pattern, remediation class, validation, and forbidden behavior.

Naming: AP-<domain>-<number>-<short-name>.md
